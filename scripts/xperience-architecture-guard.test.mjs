import { readdir, readFile } from "node:fs/promises"; import { join } from "node:path"; import assert from "node:assert/strict";
const banned=["XperienceMarketplaceBackend","XperienceAppStore","XperienceMessagingDB","XperienceWallet","XperienceIdentityDB","XperienceDrive","XperienceJobQueue","UniversalApplicationDB","DeveloperCredentialStore","CredentialRelay","FakeCapabilityBackend"];
async function files(dir){const entries=await readdir(dir,{withFileTypes:true});const result=[];for(const e of entries){if(e.name==="node_modules"||e.name==="dist"||e.name===".git")continue;const p=join(dir,e.name);result.push(...(e.isDirectory()?await files(p):[p]));}return result;}
const roots=["api","apps/xperience-console","packages/xperience-contract","packages/xperience-sdk","docs"];const source=(await Promise.all(roots.map(root=>files(join(process.cwd(),root))))).flat();const violations=[];for(const file of source){if(!/\.(ts|tsx|md|prisma|json|mjs)$/i.test(file))continue;const text=await readFile(file,"utf8");for(const name of banned)if(text.includes(name))violations.push(`${name} in ${file}`)}assert.deepEqual(violations,[]);console.log("Xperience architecture guard passed.");
