# Application manifest claims

Manifest schema version 1 includes a stable application ID, name, version, public HTTPS origin, production and Xperience URLs, capability declarations, optional GitHub repository claim, and deployment claims. It is a **claim**, not proof of control. Public HTTPS validation rejects embedded credentials and private/local network targets.

Application ownership, control evidence, capabilities, review, publication, and runtime authorization are distinct records and decisions.
