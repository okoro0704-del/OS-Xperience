import { createServer } from "node:http";
const allowed = new Set((process.env.XPERIENCE_CONSOLE_ORIGINS ?? "http://localhost:5174").split(","));
createServer((req,res)=> { const origin=req.headers.origin; if(origin&&allowed.has(origin)) res.setHeader("Access-Control-Allow-Origin",origin); res.setHeader("Vary","Origin"); res.setHeader("X-Content-Type-Options","nosniff"); res.setHeader("Content-Type","application/json"); if(req.method==="OPTIONS") { res.writeHead(204,{"Access-Control-Allow-Methods":"GET,POST","Access-Control-Allow-Headers":"Content-Type,Authorization"}); return res.end(); } res.writeHead(req.url==="/health"?200:404); res.end(JSON.stringify(req.url==="/health"?{status:"ok",service:"xperience-api"}:{error:"not_found"})); }).listen(Number(process.env.PORT ?? 4100));
